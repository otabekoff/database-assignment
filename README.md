# database-assignment

Education center relational database solution using PostgreSQL. See `docs/report.md` for the full write-up.

## Quick Start

```bash
psql -d education_center -f sql/schema.sql
psql -d education_center -f sql/data.sql
psql -d education_center -f sql/queries.sql
```
